apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: keycloak
spec:
#  tls:
#    - hosts:
#        - arch.homework
  rules:
    - host: arch.homework
      http:
        paths:
          - path: /keycloak
            pathType: Prefix
            backend:
              service:
                name: keycloak
                port:
                  number: 8080
